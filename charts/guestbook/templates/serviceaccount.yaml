{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "guestbook.fullname" .) }}
  labels:
    {{- include "guestbook.labels" . | nindent 4 }}
{{- end }}
